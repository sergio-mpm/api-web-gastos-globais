version: "3.9"

services:
  frontend:
    build: .
    container_name: api-frontend
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
      - ./env.docker.js:/usr/share/nginx/html/env.js
